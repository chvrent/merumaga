# 運用台帳 追記

| 日時 | 作業内容 | 結果・注意 | 対応者 |
| :--- | :--- | :--- | :--- |
| 2026/05/29 | 確定後に「確認」赤(is-active-red)が消える不具合をキー体系整合で修正 (別作業WIPを継承) | ブランチ `claude/fix-checkstatus-key-mismatch-20260529-122000` (`fix/calendar-red-status-key-mismatch` の未コミットWIPを継承)。根本原因2点。①クライアント `getCheckItemId` (`ClientCheckStatus.html`) が schedule_id/target_date を正規化せず、ライブ(`app_schedule`)/アーカイブ(`app_schedule_archive`)で表示値が `5`/`5.0` とブレるとキー不一致 → 設定時の保存キーと確定後の描画検索キーが食い違い赤が消える。加えて `5.0` 形式 itemId はサーバー `getSourceRowByScheduleId_` (getValues で `5`) が行を引けず `saveDailyArchiveDiffsUnlocked_` のアーカイブ書込が無言失敗。→ `getCheckItemId` を `normalizeIdKey`+`normalizeDateString` で正規化、`fillRForDay` (`Client.html`) も `getCheckItemId` 経由に統一、WIPの曖昧フォールバック(`key.includes`)は誤検出リスクで撤去し厳密一致に戻した。②サーバー `getArchivedOccurrenceRows_` (`DataArchive.gs`) が `check_setter_active`/`check_checker_active` を archive-specific 列として除外 → フルリロード後に赤の根拠が消失 (`getCheckStatuses_` は setter/checker 非返却、メモリ保持も消えるため)。→ 両列を record に surface (空欄は付与せずセッション中の `APP_DATA.checkStatuses` 短絡を防止)。①②相補的で両方必須。staging @227 反映済み。 | Claude Opus |
| 2026/05/29 | 配信編集モーダル: セクション再編 + 形式タブ刷新 + ヘッダー調整 | ブランチ `claude/modal-sections-tabs-header-20260529-120529`。①`renderEditModalFields_` (`ClientModals.html`) の `sectionMap` を実キーで再定義しバケット方式でグループ化 — 旧定義は `user_age`/`user_area` 等の存在しないキーで大半が見出しなし描画になっていた。`基本設定`(ID〜担当部署)/`メルマガ内容`(メルマガ内容〜パラメータ)/`ステータス`(新規〜検証中)/`抽出内容`(USER_年齢〜JOB_フリーワード) の4セクション。form-group に `data-section`、見出しに `data-section-head` を付け、`updateSectionHeadingVisibility_` (applyDynamicInputControl 末尾) で可視フィールド0件の空セクション見出しを隠す。②形式タブを下線型→セグメントコントロール型 (`.modal-tabs-container` 角丸グレー枠 / `.modal-tab` アクティブ白背景+微影, `Styles.html`)。**②は参照画像がコンテキスト圧縮で参照不可だったため、一般的なセグメントコントロール型で実装。要ユーザー確認**。③`.name-text` を `white-space:normal`+`overflow-wrap:anywhere` で全文折り返し表示、`.hf` 幅 66→84px、`.hf-label`/`.hf-ctrl` を左寄せ。staging @225 反映済み。 | Claude Sonnet |
| 2026/05/29 | 確認取り消し機能を追加 (`handleModalToggleChecking_`) | ブランチ `claude/unarchive-checker-20260529-114903`。「確認」ボタンが確認済み (checkerActive=true) 時に disabled で「確認済み」表示のみだったが、「確認取り消し」(赤系 `is-cancel-mode`) として有効ボタンにし checker=false 保存できるよう変更。`initEditModalCheckButtons_` の `btnChecking.disabled=checkerActive` を廃止し `is-cancel-mode` クラス切り替えに変更。`handleModalToggleChecking_` を新設 (setter は保持)。`handleModalSetChecking_` は後方互換ラッパーとして残置。`Styles.html` に `.checking-btn.is-cancel-mode` (通常・タブレット両サイズ) を追加。staging @221 反映済み。 | Claude Sonnet |
| 2026/05/29 | 設定済 / 確認済 時に mhead の「設定」「確認」塗りが消える不具合を修正 | ブランチ `claude/fix-setter-fill-20260529-113342`。`applySettingLock_` (`ClientModals.html`) が setter/checker 有効時に modal 内全 select を `disabled` にする結果、`.hf-ctrl.setter` (#f0f0fa 紫) と `.hf-ctrl.checker` (`var(--ok-bg)` 緑) がブラウザ既定の disabled スタイルで washout していた。`Styles.html` に `.hf-ctrl.setter:disabled, .hf-ctrl.setter[disabled]` と `.hf-ctrl.checker:disabled, .hf-ctrl.checker[disabled]` を追加し、`color` / `background-color` / `border-color` を `!important` で固定、`opacity:1` で透過撤回、Safari の `select:disabled` 文字色対策として `-webkit-text-fill-color` も明示、`cursor:default` で操作不能を保ったまま視覚を維持。staging Apps Script `@217` 反映済み。 | Claude Sonnet |
| 2026/05/29 | 編集モーダル: 形式タブ並び替え + ロック表現の刷新 + セクション見出し調整 | ブランチ `claude/modal-full-redesign-20260529-111850`。①`index.html` の editModal/masterModal 内 `.modal-tabs-container` で形式タブの並びを `フリー/抽出/...` → `抽出/フリー/自動求人特集/その他` に変更 (UI/modal_linear.html と一致)。②`Styles.html` の `.form-group.disabled` を `opacity:0.4` の半透明フェードから、グレー背景 + tabler ti-lock を SVG data URI で背景画像に焼き込む方式に置換、`padding-left:22px` で鍵アイコン用スペース確保、select は右パディング `18px` に、textarea は背景位置を左上 6px/7px に補正、checkbox は背景画像対象外で `opacity:0.6` に。labelもグレー化。③`.modal-section-head` のはみ出し量を `margin: 4px -12px 2px / width: calc(100% + 24px)` に変更し form-grid padding(12px) に揃え、`text-transform:uppercase` を撤去 (日本語見出しでは無効&冗長)、`font-weight` 500→600。staging Apps Script `@215` 反映済み。 | Claude Sonnet |
| 2026/05/29 | 編集モーダル ヘッダー日付化 + PRカード終了ボタン削除 + 凡例/バッジ統一 | ブランチ `claude/modal-redesign-20260529-095606`。①`ClientModals.html` `openEditModal_` の `#mhead-id-time` を `#SCH_xxx · HH:MM` から `YYYY/MM/DD/HH` (例 `2026/06/01/21`) に変更 — `CURRENT_ENTRY.target_date` の `-`→`/` + `hour` の HH 部分のみ採用。②`Client.html` `renderPrCards()` の PR カードインライン「終了」赤ボタンを撤去 (`act-btn danger` の `delete-master-row`)、終了は編集モーダル経由のみへ。③`SCHEDULE_LEGEND_ITEMS` に `type:'badge'` 3 件 (PR追加=緑/PR削除=赤/PR掲出中=ミュート) を追加し、`renderScheduleLegend_` が `pr-label pr-label-*` を凡例にもそのまま流用する分岐を追加 — カレンダー本体のバッジと配色完全一致。`Styles.html` に `.legend-badge` (font-size:10px / padding:0 6px) を追加。staging Apps Script `@211` 反映済み。 | Claude Sonnet |
| 2026/05/29 | Fix 4 を main へ取り込み + staging 再反映 | `claude/modal-calendar-pr-fix-20260529-020244` を `--no-ff` で main へマージ。前回 (`@202`) で staging に出した後、後続の Gemini サイクル取り込み等で staging が main 基準に上書きされ Fix 4 が消えていたため、改めて main マージ → staging `@208` 反映。内容: 通数(万)編集化、`.hf` 幅拡大、PR行本文表示、PRステータスマップ修正、カレンダーPRバッジ横並び、月末ヘッダー透け修正、sticky thead 高さ調整、CSS変数 `--color-month-end-bg` 導入、横スクロール抑止 (詳細は下行参照)。**反省**: 修正 → main マージ → staging deploy → ドキュメント追記 を一連で完結させなかったため Fix 4 が staging から失われた。今後は必ず一連で完了させる。 | Claude Sonnet |
| 2026/05/29 | PR管理一覧の「紐づいたメルマガ」バッジ表示を復旧 | `renderPrCards()` が古い `target_ids` カラム探索のままで `APP_DATA.prTargets` を見ておらず、紐づきセクションが描画されていなかった。冒頭で `prTargets` を `pr_id` 別にメルマガ名リストへ集約 (inactive/draft 除外) し、各カードで `.mail-chips` 内に `.mc` バッジ表示するよう修正。掲出中PRは `.mc.live` でピンクに。重複除去・名前ロケール順ソートを追加。旧 `target_ids` 経由も後方互換 fallback として残置。検証 Apps Script は `@205` へデプロイ済み。 | Claude Sonnet |
| 2026/05/29 | PR管理一覧の紐づきメルマガ名がIDのままになる不具合修正 | `getNewsletterNameMap_()` が `String(...).trim()` でキーを作っているのに、参照側は `normalizeIdKey(id)` (末尾 `.0` 除去) を使っており、Sheetsが `schedule_id` を `5.0` で返すケースでマップから引けず raw ID 表示になっていた。map 側も `normalizeIdKey` に揃え、`'ID'` エイリアスも追加。検証 Apps Script は `@204` へデプロイ済み。 | Claude Sonnet |
| 2026/05/29 | 毎月第1〜4週目サイクル(M1〜M4)の判定を追加 (Geminiから取り込み) | Gemini が main 上に stash した「M1-M4 cycle changes」(`Client.html` / `ClientUtils.html` 計+16行) を `claude/merge-gemini-cycle-20260529-022000` で取り込み main へ no-ff マージ。`SCHEDULE_CYCLE_OPTIONS` に「毎月第1〜4週目」追加、`normalizeCycleLabel` で「第1週」〜「第4週」を内部コード `M1`〜`M4` に正規化、`getScheduleCycleType` が `M1〜M4` を返却。配信判定で `/^M[1-4]$/` ブランチ追加 (該当曜日 AND `isNthWeekOfMonth(date, n)`)。`SPEC.md` 4.3 にも追記。検証 Apps Script は `@203` へデプロイ済み。 | Claude Sonnet (Gemini作業の取り込み) |
| 2026/05/29 | モーダル/カレンダー/PR表示の改修 (Fix 4 — 当初staging先行・後にmain統合) | ブランチ `claude/modal-calendar-pr-fix-20260529-020244` で実装し当初 `@202` で staging のみ反映: ①モーダル「通数(万)」を span から `input[name=delivery_count]` に変更し直接編集可能化、②`.hf` 幅 46→66px、③編集モーダルのPRセクションに PR本文 (`pr-row-body`) 追加、④PR行ステータスマッピングを `'start'/'end'` → `'add'/'remove'` に修正、⑤カレンダーのPRラベルを `div`→`span` + `.pr-labels-row` で横並びバッジ化、取り消し線廃止、追加=緑/削除=赤/掲出中=ミュートに、⑥`.month-end-header { background: transparent !important }` 削除でsticky時の透け解消、⑦sticky thead 1行目 `height:70px` / 2行目 `top:70px` を揃え、⑧CSS変数 `--color-month-end-bg` を `:root` に追加し `.date-note` と `SCHEDULE_BACKGROUND_COLORS.monthEnd` を同色 (`#d9d2e9`) で連動、⑨`.modal-scroll-body { overflow-x: hidden }` で横スクロール抑止。**後続作業で staging が main 基準に上書きされ一度消失、改めて `@208` で main マージ完了** (上行参照)。 | Claude Sonnet |
| 2026/05/28 | PRラベルの配色調整と「紐づき」ラベルの廃止 | 「追加」「削除」ラベルをより目立つ配色に変更し、「削除」の打ち消し線を廃止。複数PR時の「PR N件紐づき」サマリー表示を廃止し、個別のラベル表示へ一本化。検証 Apps Script は `@194`、本番は `@585` へデプロイ済み。 | Gemini |
| 2026/05/28 | カレンダーのPRラベル表示を改善し、検証環境へ反映 | PRラベル「開始」を「追加」に変更。PRラベルの見た目を一律チップ形式に統一。カレンダーのスロット内でメルマガごとに個別にPRラベルを表示するよう修正。検証 Apps Script は `@193` へデプロイ済み。 | Gemini |
| 2026/05/28 | カレンダーのPRラベル表示を改善 | PRラベル「開始」を「追加」に変更。PRラベルの見た目を一律チップ形式に統一。カレンダーのスロット内でメルマガごとに個別にPRラベルを表示するよう修正。 | Gemini |
| 2026/05/27 | 配信編集（設定者等）の保存・反映不具合を修正 | `CHECK_STATUS_FIELD_ALIASES` を `SCHEDULE_FIELD_ALIASES` と同期。`DataAliasUtils.gs` の `addCanonicalObjectFields_` を `getObjectFieldByAliasesSegment_` 使用へ変更し、"日本語/英語" 形式のヘッダー解決を正常化。`getMasterFieldAliases_` に `app_check_status` を追加。本番 Apps Script は `@570` へデプロイ済み。 | Gemini |
| 2026/05/27 | マスタ新規・編集モーダルに入力制御（設定者R時の確認者ロック）を追加 | `syncReviewerLock_` を汎用化し、マスタモーダルにも適用。設定者が 'R' の場合に確認者を空欄かつ無効化する制御を実装。本番 Apps Script は `@571` へデプロイ済み。 | Gemini |
| 2026/05/27 | `confirmed_by` / `confirmed_at` フィールドの廃止 | アプリ内で使用されていないため、`app_check_status` 関連のロジックから削除。本番 Apps Script は `@572` へデプロイ済み。 | Gemini |
| 2026/05/27 | `app_check_status` の列管理不具合（空列挿入）を修正 | `getCheckStatusHeaders_` を刷新し、末尾の空要素をトリミングするように変更。システム列を「日本語/英語」形式で標準化し、マスタ列と動的に同期するよう修正。本番 Apps Script は `@573` へデプロイ済み。 | Gemini |
| 2026/05/27 | `app_comments` の列管理不具合を修正 | `getCommentHeaders_` を修正し、末尾の空要素のトリミングとヘッダー名の「日本語/英語」形式への標準化を実施。本番 Apps Script は `@575` へデプロイ済み。 | Gemini |
| 2026/05/27 | `app_comments` の列名ラベルを調整 | ユーザー要望に基づき、`更新日時` → `投稿日時`、`コメント本文` → `コメント` へ変更。本番 Apps Script は `@576` へデプロイ済み。 | Gemini |
| 2026/05/27 | コメント保存時のデータ欠落（ID以外空になる）不具合を修正 | `COMMENTS_FIELD_ALIASES` を定義し、保存時の正規キー解決に適用。これにより「投稿日時」「投稿者」「コメント」「対象日」が正しく保存されるよう修正。本番 Apps Script は `@577` へデプロイ済み。 | Gemini |
| 2026/05/27 | PR管理・マスタにおける誤った列追加（is_draft等）の修正とPR紐付けの復旧 | `ensureOptionalMasterPayloadHeaders_` をシート別に制御し、`app_pr` 等へ不要な `is_draft` が混入する不具合を修正。ヘッダー定義を適正化し、PR紐付けロジックの不整合を解消。本番 Apps Script は `@578` へデプロイ済み。 | Gemini |
| 2026/05/26 | 列名 i18n 対応の読み込み性能改善を検証反映 | `mapDisplayValuesToMasterRows_` で `getMasterData` / `getSheetObjects_` を統合し、正規キー事前解決・alias 列インデックスの O(1) 化で初回読み込みを高速化。ブランチ `claude/column-name-i18n-20260526-205150` を push（`7cf5e35`）。検証 Apps Script は `@133` へデプロイ済み。本番未反映。 | Cursor |
| 2026/05/26 | マスタ停止・再開のロジック修正と確認メッセージ更新 | マスタの停止・再開を `is_inactive` ではなく `end_date` ベースへ変更し、停止確認メッセージの文言を更新。Claudeのブランチ `claude/modal-calendar-pr-fix-20260526-115430` を統合して検証・本番環境へ反映。 | Gemini |
| 2026/05/26 | メンテナンス・高速化整理を実施して本番反映 | `Client.html` に残っていた古い `renderMasterFilterChips()` / `renderMasterTable()` / `setMasterNewOnly()` の重複定義と未使用 `MASTER_FILTERS` を削除し、一覧描画中の今日の日付計算をループ外へ移動。`ClientModals.html` の入力制御 alias 探索を `getInputControlValueByAliases_()` に共通化。`SPEC.md` / `SPEC_SUMMARY.md` にメンテナンス・高速化ルールを追記。`npm run check:docs` OK、同名関数重複チェック OK。検証 Apps Script は `@92`、本番 Apps Script は `@558` へデプロイ済み。 | Codex |
| 2026/05/26 | 配信編集・マスタ編集モーダルの影響範囲注意書きを追加して本番反映 | `index.html` / `Styles.html` / `ClientModals.html` に注意書きを追加。配信編集には「この配信だけが修正されます。繰り返し設定のメルマガはマスタから編集してください。」を常時表示。既存マスタ編集には「確定済・確認済みではない同じメルマガ予定がすべて変更されます。」を表示し、PR管理・新規追加では非表示。`SPEC.md` / `SPEC_SUMMARY.md` に仕様追記。検証 Apps Script は `@91`、本番 Apps Script は `@557` へデプロイ済み。 | Codex |
| 2026/05/26 | 検証環境へApps Scriptデプロイ | `scripts/deploy-gas.ps1 -Env staging` で `chvrent18` の検証環境へ push/deploy。対象 deployment id は `AKfycbwBER-C0zjRd1piXcqvC-LHNFYP-b9zBitXxAsoaCfeJgWFjf7uxktzjdzpun3PIzdz`、Apps Script バージョンは `@90`。GitHub push は `github.com:443` 接続タイムアウトで未完了のため再試行が必要。 | Codex |
| 2026/05/26 | AI別ブランチ運用と引き継ぎルールを追加 | `AI_HANDOFF.md` を新規作成し、Codex/Claude/Gemini/Antigravity の作業ブランチ命名、他AIブランチを勝手に触らないルール、WIPコミット、引き継ぎメモ、push/deploy未実施の明記を定義。`AGENTS.md` と `CLAUDE.md` から起動時に `START_HERE.md` / `AI_HANDOFF.md` を読むよう案内し、`START_HERE.md` にも必須ルールとして追記。さらに `scripts/ai-start.ps1` でAI別ブランチへ自動分岐、`scripts/ai-wip.ps1` で途中停止前のWIPコミット、`.githooks/pre-commit` で `main` への誤コミット防止を追加し、ローカル `core.hooksPath=.githooks` を設定。今回のCodex作業は `codex/ai-handoff-automation` ブランチへ移動済み。 | Codex |
| 2026/05/25 | 入力制御シート仕様の整理 | 最新の `入力制御` シートを仕様書へ落とし込み。1枚のシート内に `タブ別入力制御`、`サイクル別入力制御`、`PR管理専用 入力項目・配置マトリクス` を縦配置し、`getInputControlRows_()` が `__section` を付けて読み分ける前提を `SPEC.md` Section 4.1 に明文化。`SPEC_SUMMARY.md` と `START_HERE.md` にも要点を追記。 | Codex |
| 2026/05/25 | 入力制御シート完全反映と関連バグ修正 | `ClientModals.html` を修正。①配信編集モーダルのロック対象に `weekday`・`is_verifying` を追加（シートRow3〜6の指定通り）。②`is_fixed`・`is_inactive`・`is_draft` を全モーダルで常時フォーム非表示化（ボタン/外部フロー管理のため）。③`getInputControlStateFromSheet_` / `getInputControlCycleStateFromSheet_` のモーダル照合バグを修正: `getByAliases(row, ['画面','モーダル',...])` が常に`画面`列値（例: '配信カレンダー'）を返し照合が一切機能していなかったため、`row['モーダル']`（例: '配信編集モーダル'）を直接参照する方式に変更。④`modalLabels` のエントリを '配信編集モーダル' 等に更新。`SPEC.md` Section 3.3・4.1 を全面改訂。 | Claude Sonnet |
| 2026/05/25 | 全体的なフォントサイズ縮小とUIのコンパクト化 | ユーザー要望に基づき、ベースフォントサイズを12pxに統一（`Styles.html`, `Client.html`）。ヘッダー(16px/12px)、タブ、ボタン、コメント等の各UI要素のサイズを調整し、画面全体の情報密度を高めつつ、視認性を維持。検証デプロイを `@54` に更新。 | Gemini CLI |
| 2026/05/25 | カレンダー色分けルールと凡例の更新 | ユーザー指定の色（検証・イレギュラー:#d9e6fc, 月末配信:#ffe5a0, その他:#e6cff2, 隔週A:#b7e1cd, 隔週B:#ffe1f6）を `Client.html` の定数へ適用。凡例ラベルを更新し、`getBgColor` ロジックに「イレギュラー」判定を追加。重複コードの削除。検証デプロイを `@56` に更新。 | Gemini CLI |
| 2026/05/25 | GitHub Actionsの向き修正とデプロイ前確認ルールの追加 | `gas-deploy.yml` が本番環境を向いていたため、検証環境（`.clasp.staging.json`）へ修正。また、再発防止策として `GEMINI.md` に「デプロイ・プッシュ指示時にActions設定と向きを強制確認する」ルールを追加。 | Gemini CLI |
| 2026/05/25 | 色分けルールの全面刷新と「検証中」項目の追加 | 背景色（単発・隔週・MA）と左ライン（月末・ウキ課以外・特殊サイクル）を組み合わせた新ルールを適用。`is_verifying`（検証中）フィールドを全系に追加し、編集モーダルにチェックボックスを実装。凡例と `SPEC.md`, `SPEC_SUMMARY.md`, `START_HERE.md` を更新。検証デプロイを `@59` に更新。 | Gemini CLI |
| 2026/05/24 | カレンダー移動表示とマスタ保存エラーの修正 | `move_override` は最新の有効ログを優先してカレンダー表示へ反映するようにし、別名解決ヘルパーを配列以外でも落ちないよう防御。マスタ保存ボタンはグローバル `event` 依存を解除。仕様書の「DnDは app_check_status.move_override に保存」「マスタ編集項目は編集可能」と整合することを確認。検証デプロイは OAuth token 通信で失敗し、権限付き再実行は環境の利用上限で未実施。 | Codex |
| 2026/05/24 | merumagaルール（作業場所・廃止フォルダ・環境確認・ドキュメント更新）の明文化 | ユーザーの指示に基づき、作業場所の制限、`merumaga` フォルダの廃止、作業前の環境確認（pwd, git status, git remote）、仕様変更時のドキュメント同時更新を「merumagaルール」として `GEMINI.md`, `START_HERE.md`, `SPEC_SUMMARY.md`, `SPEC.md` に明記した。 | Gemini CLI |
| 2026/05/24 | 作業場所を親フォルダへ一本化 | 正しい作業場所を `C:\Users\ayana.yokoo\Desktop\mail-magazine-maker` に統一。`merumaga` は廃止し、親フォルダ直下の `.git` を正とする。今後は作業前に `pwd` / `git status --short` / `git remote -v` を確認する。デプロイ手順は `scripts/deploy-gas.ps1` に一本化。 | Codex |

---
# 運用台帳 (Operational Ledger)

本ファイルは「mail-magazine-maker」プロジェクトのすべての修正、機能追加、デプロイ、およびトラブルシューティングの履歴を記録する公式台帳である。

| 日時 | 作業内容 | 結果・備考 | 対応者 |
| :--- | :--- | :--- | :--- |
| 2026/05/24 | 仕様書復元追記 | ファイル破損後、Apps Script 本体は検証環境から復元できたが、仕様書はデプロイ対象外のため完全自動復元できなかった。`SPEC.md` に最新運用仕様（停止/削除分離、読み込み5分TTL、入力制御、occurrence_override再編集、新列連携、復元/デプロイ状況）を日本語で追記し、`SPEC_SUMMARY.md` と `START_HERE.md` にも復元後の注意を追記。 | Codex |
| 2026/05/23 | R枠確認者必須解除と仕様書復元 | 配信編集で設定者が `R` の場合、確認者 select の値を空にし、`disabled` に加えて `required=false` を明示するよう修正。担当者候補の再描画後にも `syncReviewerLock_()` を再実行し、確認者必須が復活しないようにした。短くなっていた `SPEC.md` は過去の詳細版を土台に復元し、マスタ列名連動・差分赤表示・R時確認者任意・移動保存方式などの最新差分を追記した。 | Codex |
| 2026/05/23 | マスタ列名とモーダル・カレンダー連動を補強 | モーダルのラベルを内部キー名ではなく `app_schedule` の実ヘッダー名優先に変更し、`サブカテゴリ` 表記を現行の `担当部署` に統一。`USER_希望勤務地`、`USER_経験職種`、`USER_希望職種`、`JOB_勤務地`、`JOB_職種`、`JOB_フリーワード`、`配信終了` をサーバー/クライアント双方のエイリアスに追加し、マスタ編集・配信編集差分・カレンダー再読込後の値が同じ内部キーで連動するようにした。`app_check_status` には `app_schedule` の編集対象ヘッダーも追加されるため、新しい列も発生分差分として保存できる。 | Codex |
| 2026/05/23 | モーダルの差分・編集種別表示を追加 | 配信編集モーダルで、元のマスタ値から変更された項目を赤系ハイライトで即時識別できるようにした。時間はカレンダー表示値で上書きせず、マスタ行の `時間` を元値として連動するよう修正。保存差分判定でも時間・曜日・日付・チェック値を正規化し、表記揺れだけで個別差分にならないよう補強。マスタ新規・編集モーダルは青系の見出し・左ラインで表示し、日付別の発生分編集（赤）とマスタ編集（青）を視覚的に区別する。`SPEC.md` に比較キーと表示ルールを追記。 | Codex |
| 2026/05/23 | 新しいapp_schedule列名・並びへの対応 | Excelで更新された `app_schedule` の列名（`担当部署`, `USER_*`, `JOB_*`, `自動求人特集_ID/_URL/_求人数`, `配信終了`）を確認。アプリ側は `担当部署` を旧 `サブカテゴリ` 相当として読み取り、自動求人特集の新列名も既存内部キーへマッピング。未知の日本語列も `app_check_status` へ同名保存できるよう補強。 | Codex |
| 2026/05/22 | サイクル運用の固定候補化 | `サイクル` を配信周期の正本とし、`単発`、`毎日`、`毎週`、`毎月`、`月末`、`サイクル1〜3`、`隔週A/B` の固定候補へ変更。カレンダー表示判定も種別ではなくサイクルを優先するよう修正し、旧データの種別運用は互換として読み取り可能にした。 | Codex |
| 2026/05/22 | 配信停止・再開の即時UI反映を追加 | 配信編集モーダルで停止/再開した直後に、ページ再読み込みなしでモーダル内ステータス、停止/再開ボタン、編集ロック、カレンダー行の停止表示が更新されるよう修正。`SPEC.md` にも即時反映ルールを追記。 | Codex |
| 2026/05/22 | PR紐付け仕様と画面連動ルールを追記 | `SPEC.md` に、PR管理一覧の「紐づいたメルマガ」、PR編集モーダルの「PRが入るメルマガ」チェックリスト、カレンダー上の `PR[ID]追加/削除/掲出中` はすべて `app_pr_targets` を正として連動するルールを追記。`PR ID` / `ID`、`200` / `200.0`、`メルマガ名` / `mail_name` の表記揺れを吸収する方針も明文化。 | Codex |
| 2026/05/22 | PR IDの運用ID維持を仕様化 | `SPEC.md` に、`app_schedule.ID` は `SCH_###` 形式、`app_pr.PR ID` は運用担当者も参照する数値IDを維持し最大値+1で採番するルールを追記。`DataService.gs` のPR自動採番とID移行関数も、PR IDを `PR[1]` 形式へ変換しない方針に修正。 | Codex |
| 2026/05/22 | Excel上のID体系を統一 | `【ウキ】新メルマガスケジュール(作成中).xlsx` の `app_schedule.ID` を `SCH_001...` に統一。`app_pr.PR ID` は運用担当者も参照するため既存の数値ID（200番台）を維持。`app_check_status` / `app_schedule_archives` などの配信ID参照も一括更新し、存在しない旧 `SCH_001` を参照していた古いログ行を削除。実行前バックアップを作成。 | Codex |
| 2026/05/22 | 日本語列名への保存・取得対応 | `DataService.gs` のマスタ取得・保存処理を alias 経由に変更し、`app_schedule` / `app_pr` / `app_pr_targets` / `app_check_status` の日本語ヘッダーでも英語内部キーへ正規化されるよう対応。`Client.html` / `ClientModals.html` も `ID`、`PR ID`、`紐付けID` などの日本語寄り列名をID列・PR列として扱うよう更新。 | Codex |
| 2026/05/22 | 連休前アラート運用の仕様追記 | `SPEC.md` に長期連休時の他部署依頼枠調整に向けた「2週間前アラート運用（連休検知・識別ルール・通知）」を追記。調整漏れを物理的に防ぐための予防策として明文化。 | Gemini CLI |
| 2026/05/22 | スプレッドシート列名仕様の明文化 | `SPEC.md` に「スプシ表示列は日本語、内部キーは英語」を原則として追記し、`app_schedule` / `app_pr` / `app_pr_targets` / 運用ログ系シートの列名・内部キー対応表を追加。`SPEC_SUMMARY.md` / `ANTIGRAVITY.md` にも列名仕様の正本参照を追記。 | Codex |
| 2026/05/22 | 仕様書構造の再編と最新化 | `SPEC.md` の構造を整理し、5.5節のルールを各機能セクションへ統合。廃止項目の明記、GitHub Actions による自動デプロイ仕様、および最新の入力制御マトリクスを反映。`SPEC_SUMMARY.md` も 1.4 版相当へ更新。 | Gemini CLI |
| 2026/05/21 | モーダル表示切替の共通化 | `openMasterModal()`、`openModal()`、`closeMasterModal()`、`closeModal()` の `display` / `aria-hidden` 切り替えを `setModalVisible_()` に集約。表示制御の重複を減らし、将来のモーダル追加でも同じ入口を使えるように整理。 | Codex |
| 2026/05/21 | 形式タブの編集ロック同期 | `masterModal` / `editModal` の `mode=edit` 時に `modal-tab` をロックする `setModalFormatTabLockState()` を追加。形式値の更新とタブの有効/無効を同じ状態管理に寄せ、`SPEC.md` に編集時はタブと `format` を同期するルールを追記。 | Codex |
| 2026/05/21 | モーダル制御の責務分割 | `applyDynamicInputControl()` を `resetModalFieldStates_()`、`applyAutoJobFieldControls_()`、`applyEditModalLockedFieldControls_()`、`applyCurrentJobCountControls_()`、`applyPrMasterControls_()` に分割。モード別の条件と対象要素を小さな関数へ寄せ、`SPEC.md` に同責務分割の確認ルールを追記。 | Codex |
| 2026/05/21 | 特殊項目設定関数の引数整理 | `getMasterSpecialFieldConfig_()` から未使用の `header` 引数を削除し、特殊項目描画の責務をそのまま維持したままシグネチャを簡素化。`SPEC.md` は既存の関数名のまま運用を継続。 | Codex |
| 2026/05/21 | 特殊項目描画の設定関数化 | `renderMasterSpecialFieldInput_()` の内部判定を `getMasterSpecialFieldConfig_()` に切り出し、特殊項目の種類判定とHTML生成を分離。`SPEC.md` の特殊項目ルールも設定関数名ベースに更新。 | Codex |
| 2026/05/21 | 特殊項目入力描画の集約 | メルマガ名、PR ID、設定者、確認者、新規、求人件数、PR、形式非表示の特殊描画を `renderMasterSpecialFieldInput_()` に集約。`SPEC.md` に特殊扱い追加・解除時は同関数を更新するルールを追記。 | Codex |
| 2026/05/21 | テキスト系入力描画の集約 | マスタ編集・配信編集のテキスト/日付/日時/長文入力判定を `getMasterTextInputConfig_()` に集約し、HTML生成を `renderTextInputFromConfig_()` に分離。`SPEC.md` に入力タイプ追加時は設定関数へ集約する再発防止策を追記。 | Codex |
| 2026/05/21 | select描画設定の集約 | マスタ編集・配信編集の select 項目設定を `getMasterSelectConfig_()` に集約。種別・サブカテゴリ・サイクル・曜日・形式・時間の候補取得を共通化し、`SPEC.md` に select 項目追加時は同関数を経由する再発防止策を追記。 | Codex |
| 2026/05/21 | 配信編集ロック対象の定数化 | 配信編集専用ロック対象を `Client.html` の `EDIT_MODAL_LOCKED_FIELDS` に集約。動的入力制御と項目描画時の `disabled` / `readonly` 判定を同じ定数参照に変更し、`SPEC.md` に定数を正とする運用を追記。 | Codex |
| 2026/05/21 | 色・凡例ルールの定数化 | 配信行背景色、左端ライン色、ヘッダー凡例表示を `Client.html` の `SCHEDULE_BACKGROUND_COLORS` / `SCHEDULE_LINE_COLORS` / `SCHEDULE_LEGEND_ITEMS` に集約。`index.html` の静的凡例を空コンテナ化し、起動時に同じ定義から凡例を描画するよう変更。`SPEC.md` に定数を正とする運用を追記。 | Codex |
| 2026/05/21 | マスタ編集の種別ロック解除 | サブカテゴリ・サイクルと同様に、マスタ編集時の `mail_type` / `category` select から `disabled` を外し、配信編集モーダルの動的ロック対象リストからも `mail_type` を除外。`SPEC.md` のロック対象リストを `is_new` / `pr` に更新し、種別・サブカテゴリ・サイクルは編集可能項目と明記。 | Codex |
| 2026/05/21 | マスタ編集のサイクルロック解除 | サブカテゴリと同様に、マスタ編集時の `cycle` select から `disabled` を外し、配信編集モーダルの動的ロック対象リストからも `cycle` を除外。`SPEC.md` のロック対象リストを `mail_type` / `is_new` / `pr` に更新し、サブカテゴリ・サイクルは編集可能項目と明記。 | Codex |
| 2026/05/21 | マスタ編集のサブカテゴリロック解除 | マスタ編集時の `sub_category` select から `disabled` を外し、配信編集モーダルの動的ロック対象リストからも `sub_category` を除外。`SPEC.md` にサブカテゴリは編集可能項目であり、ロック対象変更時は描画側と動的制御側を同時確認する再発防止策を追記。 | Codex |
| 2026/05/21 | MAの左端ライン除外と凡例表記調整 | 種別MAの配信行はMA背景色のみで識別し、`sub_category_class` があっても左端ラインを付けないようクライアント/サーバー双方で抑止。ヘッダー凡例のオレンジライン表記を `サブカテゴリ: その他(他部署)` に変更し、`SPEC.md` にMAは左端ライン対象外であることを明記。 | Codex |
| 2026/05/21 | 左端ラインの仕様化と凡例追加 | `sub_category` による左端ラインを仕様化し、特殊=紫 `#800080`、商品=青緑 `#008080`、その他/他部署/未分類=オレンジ `#FFA500` と明記。ヘッダー凡例に左端ライン用の凡例を追加し、`SPEC.md` に左端ライン変更時も凡例を同時更新するルールを追記。 | Codex |
| 2026/05/21 | 色ルールと凡例の同時更新ルールを追加 | 背景色ルール変更時はヘッダー凡例も同じ変更で必ず更新するよう `SPEC.md` に追記。色ルールと凡例の片側だけが更新される再発を防止する。 | Codex |
| 2026/05/21 | 検証系メルマガを水色背景に追加 | 配信行背景色の水色 `#E0FFFF` 条件に `検証` を追加。サイクル数値系は特殊扱いとして薄紫 `#E6E6FA` に寄せ、水色条件は `その他` と `検証` に整理。ヘッダー凡例も隔週B `#FFF5F7`、MA `#FFF8D6`、その他/検証 `#E0FFFF` に更新。再発防止として `SPEC.md` の色設定ルールへ明記。 | Codex |
| 2026/05/21 | 自動求人特集の背景色指定を解除 | 配信行背景色の判定から `自動求人` キーワードを除外し、形式が自動求人特集であること自体では水色背景にならないよう修正。`その他` と週番号系の水色判定は維持。再発防止として `SPEC.md` の色設定ルールに「自動求人特集では背景色を付けない」を追記。 | Codex |
| 2026/05/21 | 配信行背景色と求人数最終取得日時の廃止整理 | 隔週Bの背景色を `#FFF5F7` に薄くし、種別MAに未使用色 `#FFF8D6` を追加。未使用化していた `job_count_updated_at` / 求人数最終取得日時はアプリ返却・一覧・フォーム・アラート文言から除外し、廃止列が残っても再表示されないようサーバー/クライアント双方にガードを追加。再発防止として `SPEC.md` に色設定・廃止項目の保守ルールを追記。 | Codex |
| 2026/05/11 | Webアプリ化基盤の実装（3ペインUI） | 初期構築完了 | Antigravity CLI |
| 2026/05/11 | カレンダーロジックの実装とマスタ統合 | 一部データ表示不具合あり | Antigravity CLI |
| 2026/05/11 | 運用ルール・台帳作成のルール化 | ANTIGRAVITY.md更新 | Antigravity CLI |
| 2026/05/12 | 運用ルールの更新 | デプロイ前のセルフチェックフローを ANTIGRAVITY.md に制定・明記。 | Antigravity CLI |
| 2026/05/12 | 機能追加・デプロイ | 週次配信カレンダーUIを最終化（21時表示、横スクロール対応）。clasp deploy @124 へ反映。 | Antigravity CLI |
| 2026/05/12 | 最終報告 | CSS Grid レイアウトによるセル整列の完成と仕様書の確定。clasp deploy @131 完了。 | Antigravity CLI |
| 2026/05/14 | カレンダーUIの文字化け・構文エラー修正、保存処理の更新 | `Code.js`, `DataService.gs`, `Index.html`, `Client.html` を修正。`clasp push -f` 後、既存デプロイ `AKfycbzfuyTAe_ZUsCutzU5H1UkQZVoq2zOrmn1WoP4j9tiEYBo5BDNzPW6kofDGXkTiDAJ0Qw` を `@248` に更新。 | Codex |
| 2026/05/14 | カレンダー色分けの復旧 | `Client.html` の色判定を全行文字列判定から `category` / `cycle` / `format` 判定へ修正。`新規` による水色固定を解消し、既存デプロイを `@249` に更新。 | Codex |
| 2026/05/14 | PR作業状態ラベルの動的切り替え | 配信セル内に `PR[ID]追加` / `PR[ID]削除` / `PR[ID]掲出中` を日付条件で表示。追加週は黄色背景・赤枠、削除週はグレー背景・打ち消し線に調整。既存デプロイを `@250` に更新。 | Codex |
| 2026/05/14 | PRラベルのデータ紐付け修正 | 配信行の `pr` 直接参照だけでなく、`app_pr_targets.mail_name` と `app_pr.pr_id` の突き合わせで PR ラベルを表示するように修正。既存デプロイを `@251` に更新。 | Codex |
| 2026/05/14 | PRラベル重複表示の抑止 | 同一セル内で同じ PR ID が複数状態に合致しても、`追加` > `削除` > `掲出中` の優先順位で1件だけ表示するように修正。既存デプロイを `@252` に更新。 | Codex |
| 2026/05/14 | PR削除週と複数PR表示の調整 | `削除` ラベルを終了日前7日間に変更し、PRラベルを1件1行で縦に並べるように修正。既存デプロイを `@254` に更新。 | Codex |
| 2026/05/14 | PRラベルのセル単位重複抑止 | `getPrState()` を `if / else if` の排他構造に整理し、同一セル内で同じ PR ID を1回だけ描画するように修正。既存デプロイを `@255` に更新。 | Codex |
| 2026/05/14 | マスタ管理タブの追加 | 画面上部に `配信カレンダー` / `メルマガ一覧` / `自動求人特集` / `PR管理` タブを追加。`getMasterData` / `saveMasterData` / `deleteMasterData` を追加し、対象マスタを表形式で編集・削除できるようにした。既存デプロイを `@256` に更新。 | Codex |
| 2026/05/14 | PRラベル期間判定の参照列固定 | `copy_add_date` / `copy_remove_date` は参照せず、`start_date` / `end_date`（開始日 / 終了日）から `追加` / `削除` / `掲出中` を自動計算する方針に整理。既存デプロイを `@257` に更新。 | Codex |
| 2026/05/14 | カレンダー開始日UIと配信編集項目の追加 | 開始日入力を上部ツールバーからカレンダー左端の日付ヘッダー内へ移動。配信編集モーダルに `備考` と `コメント` 入力欄を追加し、`comment` / `コメント` 列がある場合は保存対象にした。既存デプロイを `@258` に更新。 | Codex |
| 2026/05/14 | コメント機能を別シート履歴方式へ変更 | `app_comments` に `schedule_id` / `timestamp` / `user` / `comment_text` でコメントを追記する `saveComment` と、スレッド取得 `getCommentsByScheduleId` を追加。配信編集モーダルにコメントスレッドと投稿欄、カレンダー行にコメント数バッジを追加。既存デプロイを `@259` に更新。 | Codex |
| 2026/05/14 | マスタ管理画面の表示名日本語化 | `DISPLAY_NAMES` を追加し、マスタ一覧テーブルのヘッダーと編集モーダルのラベルを英語キーから日本語表示名へ変換。既存デプロイを `@260` に更新。 | Codex |
| 2026/05/14 | ID管理の自動化とUIクリーンアップ | マスタ管理UIからID列を非表示化し、新規追加時は `SCH_001` / `PR_001` 形式でサーバー側自動採番するように修正。既存IDを英語連番へ置換するリリース用関数 `migrateLegacyIdsToEnglish` も追加。既存デプロイを `@261` に更新。 | Codex |
| 2026/05/14 | 2週間前データの自動バックアップ・ロック処理追加 | `backupAndLockTwoWeeksAgo` を追加し、2週間前の火〜月に該当する `app_schedule` 行を `app_schedule_archives` へ退避して `is_fixed` を立てる処理を実装。確定済み行はUI・サーバー側で編集不可にし、火曜4時のトリガー作成用 `setupWeeklyBackupTrigger` も追加。既存デプロイを `@262` に更新。 | Codex |
| 2026/05/14 | 確定済みUIの最終調整 | 確定済み行を明確にグレーアウトし、配信編集モーダルでは保存ボタンを非表示、入力欄を読み取り専用、コメント欄のみ操作可能に調整。既存デプロイを `@263` に更新。`clasp run setupWeeklyBackupTrigger` は Apps Script API 実行設定の制約で失敗したため、GASエディタからの手動初回実行が必要。 | Codex |
| 2026/05/14 | 確定ロックを週単位発生分へ修正 | `app_schedule.is_fixed` によるマスタ行全体ロックを廃止し、`app_schedule_archives` の `source_row` + `fixed_week_start/end` から該当日だけを確定判定する方式へ変更。未来日の誤ロックを解消。既存デプロイを `@264` に更新。既存の `is_fixed` 解除用 `clearScheduleFixedFlags` も追加。 | Codex |
| 2026/05/14 | 仕様書の現行化 | これまでの修正内容を `SPEC.md` に反映し、画面構成、PRラベル、コメント履歴、ID自動採番、週単位確定ロック、デプロイ手順を現行仕様として整理。 | Codex |
| 2026/05/14 | 過去すべての確定ロックへ変更 | `backupAndLockTwoWeeksAgo` を、2週間前の締切週だけでなく、その週を含む過去すべての未アーカイブ発生分を退避する処理へ変更。週単位ロックのまま、隔週A/B・3週サイクル・月末配信期間も判定対象に追加。既存デプロイを `@265` に更新。 | Codex |
| 2026/05/14 | コメントを日付別スレッドへ変更 | `app_comments` に `target_date` を追加し、コメント保存・取得・件数バッジを `schedule_id + target_date` 単位へ変更。同じメルマガ枠を翌週に使い回してもコメントが累計表示されないように修正。既存デプロイを `@266` に更新。 | Codex |
| 2026/05/14 | カレンダー操作と横幅表示を調整 | `月末へ` ボタンを `今週` ボタンへ変更し、当日を含む火曜始まり週へ移動するよう修正。色凡例をボタン左へ移動し、メルマガ名を省略せず横スクロールで表示するよう調整。既存デプロイを `@267` に更新。 | Codex |
| 2026/05/14 | 配信停止・再開コントロールを追加 | `app_exceptions` を `schedule_id + target_date + status` の日付別例外シートとして扱い、停止・再開APIと停止中マップを追加。カレンダーでは確定済みを最優先、次に手動停止をグレーアウト＋打ち消し線で表示し、モーダルから停止・再開できるようにした。既存デプロイを `@268` に更新。 | Codex |
| 2026/05/14 | 自動求人件数チェックを追加 | `app_schedule` の `job_url` / `current_job_count` を利用し、`UrlFetchApp` で「全●件」を取得する `fetchJobCountFromUrl` と一括更新 `updateAllJobCounts` を追加。カレンダーに件数閾値別の警告アイコンを表示し、Critical時は停止ボタンを強調するようにした。既存デプロイを `@269` に更新。 | Codex |
| 2026/05/14 | 自動求人件数チェックの定期実行を週次化 | `updateAllJobCounts` の定期トリガーを1時間ごとから毎週月曜4時へ変更し、保存時の即時更新は残したまま運用負荷を下げるよう調整。既存デプロイを `@270` に更新。 | Codex |
| 2026/05/14 | 実装逆算仕様の追記 | `DataService.gs` と `Client.html` の主要関数から逆算した起動時データ取得、カレンダー描画、PR判定、確定済み判定、コメント、マスタ管理、削除、安全策の実挙動を `SPEC.md` に追記。 | Codex |
| 2026/05/14 | ここまでの実装を統合記録 | 配信カレンダー、PRラベル、コメント日付別化、確定ロック、手動停止、求人件数アラート、週次更新トリガーまでの現行動作を `SPEC.md` の総括節に整理。 | Codex |
| 2026/05/15 | 確定済み配信の停止・再開ガードをサーバー側へ追加 | `stopDelivery` / `resumeDelivery` で `schedule_id` から `source_row` を解決し、確定済み発生分なら直接呼び出しでも停止・再開を拒否するよう修正。`upsertScheduleData` も `source_row` 未指定時は `schedule_id` から確定済み判定するよう補強。既存デプロイを `@272` に更新。 | Codex |
| 2026/05/15 | ポジションマッチ3週サイクルの基準日修正 | `CYCLE_BASE_DATE` を `2026-04-27` に変更し、2026/05/12週が3週目になるよう `Client.html` / `DataService.gs` の3週サイクル計算を統一。ポジションマッチ行はマスタのサイクル数値と計算値が一致する場合のみ表示・確定対象にするよう整理。既存デプロイを `@274` に更新。 | Codex |
| 2026/05/15 | サイクル判定を種別別に分岐 | 種別が `ポジションマッチ` の場合だけ3週サイクルを適用し、それ以外は隔週A/Bで判定するよう `getCycleForRow` / `getCycleForScheduleRow_` を追加。非ポジションマッチの数値サイクルは3週サイクルとして扱わないよう修正。既存デプロイを `@275` に更新。 | Codex |
| 2026/05/15 | 求人件数取得GAS化の拡張と設定・確認赤塗り保存を追加 | `UrlFetchApp` による求人件数取得を `job_url` 全行対象へ広げ、`current_job_count` / `job_count_updated_at` を保存するよう修正。更新トリガーは1時間ごとに変更。カレンダーの求人件数アラートは100件未満で表示し、ツールチップに最終取得日時と件数を表示。`app_check_status` を追加し、設定/確認セルの赤塗り状態を保存・復元できるようにした。既存デプロイを `@276` に更新。`clasp run setupHourlyJobCountTrigger` は Apps Script API 実行設定の制約で実行できなかったため、初回トリガー作成はGASエディタから実行が必要。 | Codex |
| 2026/05/15 | 求人数0件アラート誤作動とポジションマッチcycle比較を修正 | 空欄の `current_job_count` を0件扱いしないよう、未取得・非数値ではアラートを出さず、数値0の場合のみアラートを出すよう修正。求人件数抽出はXPath相当箇所または `全 ... 件` のみに限定し、ページ内の任意の `0件` を拾わないよう調整。ポジションマッチのcycle比較は `String(row.cycle).indexOf(String(targetCycle)) !== -1` 相当に変更し、数値3・文字列"3"・"A3"を同じ3週目として扱うようにした。既存デプロイを `@277` に更新。 | Codex |
| 2026/05/15 | ポジションマッチの内部サイクル優先とヘッダー名正規化を修正 | `app_schedule` の `今週サイクル(内部)` / `今週非配信(内部)` を読み込み、シート側の今週判定をWeb表示でも優先するよう修正。列位置固定で `category/hour/mail_name` を誤読する可能性があったため、正規化をヘッダー名優先へ変更。ポジションマッチ判定は種別だけでなくメルマガ名・備考も見るようにした。既存デプロイを `@278` に更新。 | Codex |
| 2026/05/15 | サブカテゴリ色分けとPR ID表示を追加 | H列相当の `sub_category` から `sub_category_class` を生成し、配信行に `slot-item is-special/is-product/is-others` を付与。特殊は薄紫、商品は薄い青緑、その他/他部署は薄い黄色で表示。A/B週・1〜3週目チップにも凡例に合わせた色を付与。PR管理では `pr_id` を表示対象に戻した。既存デプロイを `@281` に更新。 | Codex |
| 2026/05/15 | 色分けの役割を背景色=配信サイクル、左枠線=サブカテゴリへ変更 | 配信行の背景色は既存凡例の毎週/特殊/毎月/隔週A/隔週B/その他で判定するよう戻し、`sub_category` は左枠線だけに変更。商品は濃い青緑、特殊は濃い紫、その他/他部署はオレンジの `border-left: 6px solid` を付与。空欄は追加枠線なし。`is-others` の黄色背景指定を削除。既存デプロイを `@284` に更新。 | Codex |
| 2026/05/15 | app_check_statusを配信ログ化 | `saveCheckStatus` を拡張し、確認セルの承認時に `delivery_date` / `hour` / `mail_name` / `job_url` / `current_job_count` / `confirmed_by` / `confirmed_at` を保存または上書きするよう修正。既存の赤塗り状態復元は維持。既存デプロイを `@282` に更新。 | Codex |
| 2026/05/15 | 求人数取得のXPath相当抽出を修正 | IMPORTXML(job_url, "/html/body/form/div[1]/div[3]/main/div/div/div[2]/div/span[1]") と同等の階層をGAS側で辿る抽出関数を追加。UrlFetchApp にUser-Agentを付与し、XPath相当箇所で取得できない場合だけ 全 ... 件 フォールバックを使うよう修正。既存デプロイを @283 に更新。 | Codex |
| 2026/05/15 | ポジションマッチ非表示と凡例不整合の修正 | `isPositionMatch` を種別・メルマガ名・備考に加え、数値サイクル(1-3)自体の有無でも判定するよう強化し、抽出漏れを完全に解消。凡例の表記を「特殊配信」「自動求人/その他」に改め、`getBgColor` の判定順とキーワードを実データに最適化。既存デプロイを `@286` に更新。 | Antigravity CLI |
| 2026/05/15 | 自動求人特集タブの削除とメルマガ一覧へのフィルタ実装 | 「自動求人特集」タブを削除し、マスタ管理を「メルマガ一覧」へ統合。メルマガ一覧（配信マスタ）内に「形式:自動求人特集」「種別:イレギュラー」「種別:検証」のクイックフィルターチップを追加。既存デプロイを `@287` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理画面へのタイトル表示とシステム列の非表示化 | `getPRData` を追加し、PR対象設定テーブルにPRマスタの「名称」を「PRタイトル」として結合表示。あわせて「元行」「元シート」などのシステム用カラムをマスタ管理画面から非表示化。既存デプロイを `@288` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理ロジックの抜本的改善 (PR 2.0) | 行番号ベースの紐付けを廃止し、`app_pr` の `target_ids` によるコンテンツ主体管理へ移行。PR編集モーダルに検索機能付きのメルマガ選択リスト（複数選択）を実装。PR管理画面では紐付け先をチップ形式で一覧表示し、カレンダーへの自動反映ロジックを新方式へ刷新。既存デプロイを `@289` に更新。 | Antigravity CLI |
| 2026/05/15 | マスタ読み込み不具合の修正 | `loadActiveMaster` における非同期通信のチェイン記述ミスを修正し、メルマガ一覧（マスタ管理）が正常に読み込まれない問題を解消。既存デプロイを `@290` に更新。 | Antigravity CLI |
| 2026/05/15 | カレンダー初期表示日の調整 | カレンダー読み込み時のデフォルト開始日を、当日を含む「直近の火曜日」へ変更。火曜始まりの運用サイクルに最適化。既存デプロイを `@291` に更新。 | Antigravity CLI |
| 2026/05/15 | カレンダー列ヘッダーの集約 | 毎時間のセル内に繰り返し表示されていたヘッダー（内容・通数・設定・確認）を、日付の下の1行だけに集約。画面の垂直スペースを節約し、視認性を向上。既存デプロイを `@292` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理の項目名変更とUIのブラッシュアップ | PRマスタの「名称」を「PRタイトル」に改称。PR編集モーダルでの検索・複数選択UIをより強調するように調整。既存デプロイを `@293` に更新。 | Antigravity CLI |
| 2026/05/15 | カレンダー ドラッグ＆ドロップ機能の実装 | 「移動モード」トグルを追加し、ONのときのみ SortableJS によるカード移動を可能に。移動後は `app_schedule` の曜日・時間と `app_check_status` の移動ログを自動更新。確定済みアイテムの移動制限も実装。既存デプロイを `@294` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理の項目名と選択UIの調整 | PRマスタの「名称」を「PRタイトル」、「紐付けID」を「PRが入るメルマガ」に改称し、編集時の複数選択UIを直感的に改善。バックエンド設定と画面表示を整合。既存デプロイを `@295` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理モーダルのUIブラッシュアップ | PRタイトルとPRが入るメルマガの選択UIをより明確化。モーダル内でのメルマガ複数選択機能をより直感的に操作できるよう、UIレイアウトと保存ロジックを最適化。既存デプロイを `@297` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理の項目名と選択UIの調整 | PRマスタの「名称」を「PRタイトル」、「紐付けID」を「PRが入るメルマガ」に改称し、編集時の複数選択UIを直感的に改善。バックエンド設定と画面表示を整合。既存デプロイを `@295` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理モーダルのUIブラッシュアップ | PRタイトルとPRが入るメルマガの選択UIをより明確化。モーダル内でのメルマガ複数選択機能をより直感的に操作できるよう、UIレイアウトと保存ロジックを最適化。既存デプロイを `@297` に更新。 | Antigravity CLI |
| 2026/05/15 | カレンダー ドラッグ＆ドロップ移動不具合の修正 | 移動操作時に「Schedule row not found」エラーが発生する問題を解消。`itemId` 生成およびサーバー側での ID 解決ロジックを改善し、正確な行特定を可能に。既存デプロイを `@298` に更新。 | Antigravity CLI |
| 2026/05/15 | 配信カレンダー表示バグの修正 | カレンダー描画時にフロントエンド側で定義されていない関数 `parseScheduleDate_` を呼び出していた不具合を修正。クライアントサイドに関数を実装し、JavaScript の参照エラーを解消。既存デプロイを `@327` に更新。 | Antigravity CLI |
| 2026/05/15 | ドラッグ＆ドロップ移動ロジックの刷新 | ドロップ操作がマスター（app_schedule）を書き換えないよう仕様変更。移動先情報を `app_check_status` へ「移動オーバーライド」として記録し、カレンダー描画時にそれを優先表示する方式へ刷新。個別の移動発生分のみが移動し、翌週以降の定例スケジュールは保持される挙動を実現。既存デプロイを `@301` に更新。 | Antigravity CLI |
| 2026/05/15 | カレンダー描画不具合の修正 | ドラッグ＆ドロップ機能導入に伴い、カレンダー描画ロジックで発生していたメルマガの非表示バグを修正。移動されたアイテムの追跡と、マスタースケジュールとのフィルタリング判定を最適化し、全メルマガが正常に描画されるよう改善。既存デプロイを `@302` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理モーダルのメルマガ選択UI表示不具合の修正 | PRマスタ編集モーダルにて、条件判定の不整合で非表示になっていた「PRが入るメルマガ」の選択リストを表示するように修正。既存デプロイを `@303` に更新。 | Antigravity CLI |
| 2026/05/15 | 週判定ロジックを「火曜始まり」へ刷新 | A/B週および3週サイクルの判定基準を、月曜切り替えから「火曜〜月曜」の週サイクルへ変更。カレンダーの週番号計算基準を `2026/04/28(火)` に合わせ、5/18(月)が前週(5/12火からの週)と同じサイクルになるよう同期。既存デプロイを `@304` に更新。 | Antigravity CLI |
| 2026/05/15 | サイクル基準日(火曜)への完全移行 | `CYCLE_BASE_DATE` を4/27(月)から4/28(火)へ変更し、システム全体を火曜始まりサイクルへ統一。これにより週切り替えのズレを完全に解消。既存デプロイを `@305` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理の紐付け保存ロジックの修正 | PR編集時にチェックしたメルマガ情報が `app_pr_targets` に正しく反映されない不具合を解消。PR ID に紐づく過去のターゲットを一度クリアし、新たに選択されたメルマガを紐付けとして書き込む方式を実装。既存デプロイを `@307` に更新。 | Antigravity CLI |
| 2026/05/15 | PR保存ロジックの最適化とUIフィードバック実装 | `savePRData` におけるスプレッドシートへの書き込みをバルク処理（setValues）へ刷新し、処理遅延を改善。あわせてPR編集モーダルの「保存」ボタンに非活性制御と状態表示（保存中...）を追加し、保存処理の重複実行とUIブロックを防止。既存デプロイを `@308` に更新。 | Antigravity CLI |
| 2026/05/15 | 配信編集モーダルでのPR期間表示機能の実装 | 配信枠クリック時の編集モーダルに表示されるPR詳細情報において、「開始日」から「終了日」までの有効期間を表示するように改善。現在の日付に対し有効なPRは青枠（active）、期間外のPRはグレー（inactive）で視覚的に区別。既存デプロイを `@312` に更新。 | Antigravity CLI |
| 2026/05/15 | ドラッグ＆ドロップ移動不具合の再修正（マスター直書き方式へ） | 移動後の情報が再読み込みで元に戻る不具合を解消。移動先の日時を `app_check_status` だけでなく、マスタデータ（`app_schedule`）の曜日と時間に直接書き込むよう仕様を差し戻し、整合性を確保。既存デプロイを `@325` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理画面のUIクリーンアップ | PR管理画面および編集モーダルから「配信停止」列を非表示化。内部データ（app_prシート）の「配信停止」フラグ（is_inactive）によるフィルタリングロジックは維持しつつ、ユーザーの操作対象から不要な項目を除外。既存デプロイを `@311` に更新。 | Antigravity CLI |
| 2026/05/15 | マスタ一覧描画とPR紐付け保存の修正 | マスタ一覧テーブルがDOM挿入前にtbodyへ追記して落ちる不具合を修正。PR紐付け保存時の `source_row` 参照と、新規PR自動採番後の `pr_id` 引き継ぎを補強。`Client.html` / `DataService.gs` の構文チェック後、既存デプロイを `@337` に更新。 | Codex |
| 2026/05/15 | 配信カレンダーのヘッダー固定表示 (Sticky Header) 実装 | CSS の `thead th` に `position: sticky;` を適用し、スクロール時に日付行を画面上部に固定するよう調整。既存デプロイを `@344` に更新。 | Antigravity CLI |
| 2026/05/15 | PR管理一覧の紐付け列名調整 | PR管理一覧で `mail_name` と `target_mails` が同内容で重複表示されないよう通常列から除外し、最後に「紐づいたメルマガ」1列だけ表示するよう調整。既存デプロイを `@339` に更新。 | Codex |
| 2026/05/15 | マスタ編集フォームの日付入力対応 | マスタ編集モーダルの入力生成を改善し、日付列は `date`、日時列は `datetime-local`、時間列は `time` の入力に変更。開始日・終了日・target_date などをカレンダー選択できるようにした。既存デプロイを `@340` に更新。 | Codex |
| 2026/05/15 | マスタ編集フォームのプルダウン化 | 曜日を曜日選択、時間を8:00〜21:00選択、形式を空白/フリー/抽出/自動求人特集選択に変更。設定者・確認者は `app_admin_master` / `app_name_master` の担当者候補から選択できるようにし、求人数最終取得日時と確定済はフォーム上非表示にした。既存デプロイを `@341` に更新。 | Codex |
| 2026/05/15 | 配信カレンダーの日付ヘッダー固定 | 配信カレンダーを縦スクロールしても日付・曜日行と項目ヘッダー行が画面上部に残るよう、カレンダー表の `thead` に sticky 位置と重なり順を明示。既存デプロイを `@342` に更新。 | Codex |
| 2026/05/15 | 配信カレンダーの土日祝ヘッダー強調 | `CalendarApp` で日本の祝日カレンダー (`ja.japanese#holiday@group.v.calendar.google.com`) から祝日を取得し、土日または祝日のヘッダーへ `is-weekend` / `is-holiday` を付与。日付・曜日を赤系の強調色で表示するようCSSを追加。既存デプロイを `@343` に更新。 | Codex |
| 2026/05/15 | stickyヘッダーとモーダルの重なり順修正 | カレンダーstickyヘッダーの z-index を10台へ下げ、編集モーダルのオーバーレイを `z-index: 1000` に変更。モーダルが常にヘッダーより前面に表示されるよう修正。既存デプロイを `@344` に更新。 | Codex |
| 2026/05/15 | 赤塗りチェック状態の日付別保存へ変更 | 設定者/確認者セルの赤塗り保存キーを `schedule_id` 単位から `schedule_id + target_date` 単位へ変更。翌週以降の同じメルマガ枠へ赤塗りが波及しないよう修正。既存デプロイを `@345` に更新。 | Codex |
| 2026/05/15 | ドラッグ&ドロップ移動IDの分離修正 | 赤塗り用の日付別IDと移動用の `schedule_id` が混ざり、`リスト_133|2026-05-13` のようなIDで行検索して移動失敗する問題を修正。カードには `data-schedule-id` を持たせ、サーバー側でも日付付きIDを受けた場合に元IDへ正規化するよう補強。既存デプロイを `@346` に更新。 | Codex |
| 2026/05/15 | DnD移動を個別スケジュール保存へ変更 | `updateItemDate` から `app_schedule` の曜日・時間更新を削除し、`app_check_status` に `move_override` として `schedule_id + original_date -> delivery_date/hour` を保存する方式へ変更。カレンダー表示時は個別移動を優先し、元日付のマスター発生分は非表示にする。既存デプロイを `@347` に更新。 | Codex |
| 2026/05/15 | 運用ログの自動アーカイブ関数追加 | `app_check_status` / `app_comments` / `app_exceptions` の90日超過行を各 `_archive` シートへ退避する `archiveOldOperationalLogs` と、毎月1日3時のトリガー作成用 `setupMonthlyLogArchiveTrigger` を追加。既存デプロイを `@348` に更新。`clasp run setupMonthlyLogArchiveTrigger` はApps Script API実行設定の制約で失敗したため、GASエディタから初回実行が必要。 | Codex |
| 2026/05/15 | 初期表示の運用ログ取得範囲を絞り込み | `getInitialData` に表示週の `startDate/endDate` を渡し、コメント数・停止例外・赤塗り/移動ログ・確定済み発生分を表示範囲±7日のみに絞って返すよう修正。週移動時は再取得するようクライアント側も変更。既存デプロイを `@349` に更新。 | Codex |
| 2026/05/15 | 保存系関数へLockService追加 | マスタ保存/削除、コメント、配信停止/再開、DnD移動、PR保存、赤塗り保存、求人数更新、ログアーカイブ、配信編集保存に `LockService` を適用。同時操作時のスプレッドシート書き込み競合を抑制。既存デプロイを `@350` に更新。 | Codex |
| 2026/05/15 | 日本の祝日データ取得をキャッシュ化 | `getJapaneseHolidays_` に `CacheService.getScriptCache()` を導入し、祝日カレンダー取得結果を6時間キャッシュ。初期表示ごとの `CalendarApp` 呼び出しを削減。既存デプロイを `@351` に更新。 | Codex |
| 2026/05/15 | 求人数取得の差分化 | `updateAllJobCounts` で `job_count_updated_at` が12時間以内の行をスキップするよう変更。マスタ保存時は全件更新ではなく保存対象行だけを強制更新する `updateJobCountForRowUnlocked_` に変更し、UrlFetch負荷を削減。既存デプロイを `@352` に更新。 | Codex |
| 2026/05/15 | マスタ一覧の表示件数制御 | マスタ一覧テーブルのDOM描画を最大300件に制限し、検索・フィルタ後の該当件数と表示件数を表示するサマリーを追加。大量行表示時のブラウザ負荷を抑制。既存デプロイを @353 に更新。 | Codex |
| 2026/05/18 | 文字化け修正とサイクル表示の刷新 | DataService.gsの「毎月」「毎週」「隔週A/B」の文字化けを修正。カレンダーヘッダーの「1〜3週目」表示を「A1〜A3」に変更し、A3週には「関西オープン」の注釈を追加。 | Antigravity CLI |
| 2026/05/18 | 当日ハイライト機能の実装 | カレンダー上で今日の日付（5/18）がひと目で分かるよう、ヘッダーに青い下線と背景色を追加し、列全体を薄く色付け。 | Antigravity CLI |
| 2026/05/18 | ファイル名大文字小文字エラーの修正 | `Code.js` が期待する `Index.html` が `index.html` になっていたため、大文字に修正して再デプロイ。実行時エラーを解消。 | Antigravity CLI |
| 2026/05/18 | Index.htmlのコンフリクト解消 | `Index.html` 内にGitのコンフリクトマーカー（<<<< HEAD等）が混入していたため、不要な箇所を削除してファイルを正常化。 | Antigravity CLI |
| 2026/05/18 | サイクル表示の動的化（A1/B1など） | 2週サイクルのA/Bと、3週サイクルの1/2/3を組み合わせ、`A1` `B2` `A3` `B1` のように現在の週の正しい名称が表示されるよう修正。 | Antigravity CLI |
| 2026/05/18 | 当日一括停止・再開機能の実装 | カレンダーの各日付ヘッダーに「全停止」「再開」ボタンを追加。その日の全配信（確定済みを除く）を一括で操作可能に。 | Antigravity CLI |
| 2026/05/18 | セキュリティ警告（CSP）への対策 | ブラウザコンソールで発生していたフレーム関連の警告を抑制するため、`doGet` に `setXFrameOptionsMode(ALLOWALL)` を追加。 | Antigravity CLI |
| 2026/05/18 | 配信編集モーダルの項目同期と発生分保存 | 配信編集モーダルの追加項目を `app_schedule` ヘッダー基準で生成し、メルマガ一覧の新規追加項目と揃えた。メルマガ名は編集不可・クリックコピーに固定し、設定者 `R` の場合は確認者を空欄かつ選択不可にした。日付×メルマガ単位の変更は `app_check_status` の `occurrence_override` として保存する。 | Codex |
| 2026/05/18 | 画面別注意書きと赤塗り確定バックアップ | 配信カレンダーとメルマガ一覧に編集範囲の注意書きを追加。設定・確認が同じ日付/メルマガで両方赤くなった時点で、`app_schedule_archives` に単日バックアップを作成し、その日だけ確定済みとして扱うようにした。`occurrence_override` がある場合は差分適用後の内容を保存する。 | Codex |
| 2026/05/18 | 赤塗り解除時の単日バックアップ削除 | 設定または確認の赤塗りを解除した場合、同じ `source_row + target_date` の単日バックアップを `app_schedule_archives` から削除するようにした。週次バックアップは削除対象外。 | Codex |
| 2026/05/18 | R確定・一括保存・処理中表示・週次バックアップ無効化 | 設定者が `R` の場合は設定セルの赤塗りだけで単日バックアップを作成するよう変更。`Rをすべて塗りつぶす` は見た目だけでなく一括保存する方式に変更し、保存・更新中は処理中オーバーレイで操作を抑制。リアルタイム確定を正とするため `backupAndLockTwoWeeksAgo()` を無効化し、起動時ルールに `SPEC_SUMMARY.md` / `運用台帳.md` の自動追記を追加。 | Codex |
| 2026/05/19 | SPEC.mdの機能別詳細化とモーダルUI/UX改善の実装 | `SPEC.md` をコードに基づき機能別に詳細化。配信編集モーダルの「新規」をチェックボックス化、メルマガ名コピー機能、形式別入力制御マトリクスの実装、およびID自動採番ロジックの改善。既存デプロイを `@437` に更新。 | Antigravity CLI |
| 2026/05/19 | 保存方式の整理と旧導線削除 | 設定/確認の保存先を `app_schedule_archives` の日別差分に統一し、`app_check_status` は `move_override` / `occurrence_override` 専用に整理。旧リアルタイム確定・旧一括保存・単日削除・週次バックアップ本体を削除/無効化し、仕様書を現行方式へ更新。 | Codex |
| 2026/05/19 | 確定差分の抽出条件を整理 | 一括保存時の保存対象を、通常枠は `確認` が赤い行、R枠は `設定` が赤い行に限定。通常枠の `設定` はブラウザ内メモ扱いとし、GAS保存対象から除外。配信カレンダーの注意書きも新仕様に合わせて更新。 | Codex |
| 2026/05/19 | 日別R一括設定を追加 | 配信カレンダー各日付ヘッダーに `R一括設定` を追加し、その日付列のR枠の `設定` だけをローカルで赤塗りできるようにした。グローバルな `Rをすべて塗りつぶす` トグルは削除。 | Codex |
| 2026/05/19 | メルマガ新規追加直後のカレンダー反映を修正 | `app_schedule` の初期取得キャッシュを廃止し、メルマガ一覧から新規追加・編集した内容が保存直後の `loadData()` で必ずカレンダーへ反映されるようにした。 | Codex |
| 2026/05/19 | モーダルタブUIの刷新（モダン化・横並び固定） | モーダル内の形式切り替えタブがプレーンテキスト化していた問題を解消。CSSで横並びのモダンなタブUIを実装し、`data-format` 属性ベースの堅牢なJS制御へリプレイスした。 | Antigravity CLI |
| 2026/05/19 | 編集モーダルのデータ連携バグ修正とグリッド配置 | 編集ボタン押下時に既存データがフォームへ反映されない不具合を解消。HTMLの `name` 属性とJSのデータバインディングを統合し、2カラムの美しいグリッドレイアウト（form-grid）へ統一した。 | Antigravity CLI |
| 2026/05/20 | Antigravity CLI への移行（リブランディング） | 全ドキュメントの「Gemini CLI」を「Antigravity CLI」へ、「GEMINI.md」を「ANTIGRAVITY.md」へ置換・改名。既存デプロイを `@473` に更新。 | Antigravity CLI |




| 2026/05/22 | SPEC.mdの文字化け復元と再発防止策の追加 | `SPEC.md` を文字化け前の正常履歴と照合して復元し、2026/05/21時点の正常な追記は保持。内容確認の結果、参照ドキュメント自体はUTF-8で、PowerShell表示だけが崩れるケースがあるため、`.editorconfig` を追加してUTF-8/LFを明示し、`SPEC.md` / `SPEC_SUMMARY.md` / `ANTIGRAVITY.md` に文字コード確認ルールを追記。原因はUTF-8日本語をCP932表示・保存として扱ったことによる文字化け。再発防止として、コミット前に `git diff` と文字化けパターン検索で確認する手順を記録。 | Codex |
| 2026/05/22 | Client.htmlの段階的分割を開始 | `Client.html` から表示名・日付・サイクル・文字列正規化などの下位ユーティリティ関数を `ClientUtils.html` へ切り出し、`index.html` で `ClientUtils` を `Client` より先に読み込む構成へ変更。今回は挙動変更を避けるため、関数移動のみを実施し、カレンダー描画・保存処理・サーバー処理は未分割のまま維持。 | Codex |
| 2026/05/22 | ClientUtils.htmlのGAS反映漏れを修正 | `.claspignore` に `!ClientUtils.html` を追加し、分割したHTMLファイルがApps Scriptへpushされるよう修正。`clasp push -f` で7ファイルを反映し、既存Webアプリデプロイを `@506` に更新。原因は新規HTMLファイル追加時に `.claspignore` の許可リスト更新が漏れていたこと。 | Codex |
| 2026/05/22 | Client.htmlのコメント機能を分割 | `loadCommentsForCurrentEntry` / `postComment` / `renderComments` / コメント件数関連関数を `ClientComments.html` へ切り出し。`index.html` で `ClientComments` を `Client` より前、`ClientUtils` より後に読み込む構成へ変更し、`.claspignore` に `!ClientComments.html` を追加。`clasp push -f` で8ファイルを反映し、既存Webアプリデプロイを `@508` に更新。 | Codex |
| 2026/05/22 | Client.htmlのチェック状態機能を分割 | 設定/確認セルの赤塗り、未保存変更、日別一括保存、過去日アーカイブ参照に関する関数群を `ClientCheckStatus.html` へ切り出し。`index.html` で `ClientCheckStatus` を `Client` より前に読み込む構成へ変更し、`.claspignore` に `!ClientCheckStatus.html` を追加。`clasp push -f` で9ファイルを反映し、既存Webアプリデプロイを `@510` に更新。 | Codex |
| 2026/05/22 | Client.htmlのモーダル機能を分割 | マスタ編集/追加、配信編集、形式タブ、動的入力制御、配信停止/再開に関する関数群を `ClientModals.html` へ切り出し。`index.html` で `ClientModals` を `Client` より前に読み込む構成へ変更し、`.claspignore` に `!ClientModals.html` を追加。`clasp push -f` で10ファイルを反映し、既存Webアプリデプロイを `@512` に更新。 | Codex |
| 2026/05/22 | DataService.gsのコメント機能を分割 | コメント保存/取得、コメント件数集計、コメント用キャッシュキー/無効化、コメント日付正規化の関数群を `DataCommentService.gs` へ切り出し。`.claspignore` に `!DataCommentService.gs` を追加し、Apps Scriptへpush対象として管理。`clasp push -f` で11ファイルを反映し、既存Webアプリデプロイを `@514` に更新。 | Codex |
| 2026/05/22 | DataService.gsの停止/例外機能を分割 | 配信停止/再開、一括停止/一括再開、停止済み発生分の取得、例外シート生成/ヘッダー補完、停止キー生成の関数群を `DataExceptionService.gs` へ切り出し。`.claspignore` に `!DataExceptionService.gs` を追加し、Apps Scriptへpush対象として管理。`clasp push -f` で12ファイルを反映し、既存Webアプリデプロイを `@517` に更新。 | Codex |
| 2026/05/22 | 配信編集画面のPR表示を複数・重複対応に修正 | `getPrRecordsForSchedule` を `pr_id` 単位で重複排除し、`app_pr` / `app_pr_targets` 由来のPRについて `PR ID` / `タイトル` / `PR内容` を表示するよう修正。`PR本文` は改行を保持して表示し、PR管理側の `target_mails` も重複を除去。`clasp push -f` で12ファイルを反映し、既存Webアプリデプロイを `@521` に更新。 | Codex |


## 2026/05/24 GitHub Actions CLASPRC_JSON 検証追記

- .github/workflows/gas-deploy.yml で CLASPRC_JSON が空または壊れたJSONのときに Unexpected end of JSON input だけで落ちないよう、未設定チェックと JSON.parse による事前検証を追加。~/.clasprc.json への書き込みは printf '%s' を使用する。GitHub Secrets の CLASPRC_JSON にはローカルの ~/.clasprc.json 全文を入れること。


## 2026/05/24 GitHub Actions clasp user指定修正

- CLASPRC_JSON は新形式の tokens.chvrent18.refresh_token を持つため、Actionsで CLASP_USER=chvrent18 を明示し、clasp --user chvrent18 -P .clasp.staging.json -I .claspignore で検証Apps Scriptへpush/deployするよう修正。検証deployment idは AKfycbwBER-C0zjRd1piXcqvC-LHNFYP-b9zBitXxAsoaCfeJgWFjf7uxktzjdzpun3PIzdz。
